import styled from 'styled-components'

export const EVStyle = styled.div`
    min-height: 100vh;
    width: 100vw;
    background-color: rgba(0,0,0,0.1);
    display: grid;
    place-items: center;
    perspective: 1000px;
    
    img {
        box-shadow: 0px 5px 10px rgba(0,0,0,0.5);
    }
`